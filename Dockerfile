FROM rocker/hadleyverse
MAINTAINER Jeff Walker

EXPOSE 8787

RUN echo "r <- getOption('repos'); r['CRAN'] <- 'http://cran.us.r-project.org'; options(repos = r);" > ~/.Rprofile
RUN Rscript -e "install.packages('lubridate')"
